# None type
#
#
# None:
#
# This is a singleton object used to signal that a value is absent, not
# applicable, missing, not found, lack of answer, uninitialised
#
assert None is None
#
a = None
assert a is None
#
x = [1, 2, 3]
assert x is not None